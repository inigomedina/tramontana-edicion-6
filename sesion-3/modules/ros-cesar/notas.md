# Notas

El documento thinking.md está muy bien, se nota que pensasteis el modelo antes de codificar. La separación de Markets como entidad propia tiene sentido.

Cosas a mejorar:

1. La carpeta `My Model ` tiene un espacio al final del nombre, eso puede dar problemas. Renombradla a algo como `model` (sin espacios)
2. El servidor betactivity.py parece incompleto, falta terminar la implementación

El análisis de heavy_users.py está bien planteado. Completad el servidor y tendréis el ejercicio cerrado.
